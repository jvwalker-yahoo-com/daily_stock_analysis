name: Daily Stock Analysis

on:
  schedule:
    - cron: '0 7 * * 1-5'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  analyze:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies & Chinese fonts
        run: |
          pip install -r requirements.txt || true
          pip install reportlab requests yfinance pandas google-genai
          sudo apt-get update && sudo apt-get install -y fonts-wqy-microhei

      - name: Run analysis dynamically in chunks from stock_list.txt
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          TAVILY_API_KEYS: ${{ secrets.TAVILY_API_KEYS }}
          SERPAPI_API_KEYS: ${{ secrets.SERPAPI_API_KEYS }}
          ANPIRE_API_KEY: ${{ secrets.ANPIRE_API_KEY }}
          ALPHA_VANTAGE_API_KEY: ${{ secrets.ALPHA_VANTAGE_API_KEY }}
          FINNHUB_API_KEY: ${{ secrets.FINNHUB_API_KEY }}
          FMP_API_KEY: ${{ secrets.FMP_API_KEY }}
          TWELVE_DATA_API_KEY: ${{ secrets.TWELVE_DATA_API_KEY }}
        run: |
          python3 -c "
          import os
          import time
          from src.core.pipeline import StockAnalysisPipeline
          from src.config import get_config
          
          config = get_config()
          stocks = []
          if os.path.exists('stock_list.txt'):
              with open('stock_list.txt', 'r', encoding='utf-8') as f:
                  for line in f:
                      ticker = line.strip()
                      if ticker and not ticker.startswith('#'):
                          stocks.append(ticker)
          
          print(f'Dynamically loaded stock list: {stocks}')
          
          chunk_size = 5
          stock_chunks = [stocks[i:i + chunk_size] for i in range(0, len(stocks), chunk_size)]
          pipeline = StockAnalysisPipeline(config=config)
          
          for idx, chunk in enumerate(stock_chunks):
              print(f'=== Processing Chunk {idx+1} of {len(stock_chunks)}: {chunk} ===')
              try:
                  pipeline.run(stock_list=chunk)
              except Exception as e:
                  print(f'Error processing chunk {chunk}: {e}')
              
              if idx < len(stock_chunks) - 1:
                  time.sleep(10)
          "

      - name: Generate Dual-Language (EN & CN) USD PDFs & Email
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          NOTIFY_EMAIL: ${{ secrets.NOTIFY_EMAIL }}
        run: |
          python3 - << 'EOF'
          import os
          import re
          import json
          import sqlite3
          import smtplib
          import yfinance as yf
          from datetime import datetime, date
          from google import genai
          from email.mime.text import MIMEText
          from email.mime.multipart import MIMEMultipart
          from email.mime.base import MIMEBase
          from email import encoders
          
          from reportlab.lib.pagesizes import letter
          from reportlab.platypus import SimpleDocTemplate, Paragraph, Spacer, Table, TableStyle
          from reportlab.lib.styles import getSampleStyleSheet, ParagraphStyle
          from reportlab.lib import colors
          from reportlab.pdfbase import pdfmetrics
          from reportlab.pdfbase.ttfonts import TTFont

          # 1. Register Fonts
          font_path = "/usr/share/fonts/truetype/wqy/wqy-microhei.ttc"
          if os.path.exists(font_path):
              pdfmetrics.registerFont(TTFont('ChineseFont', font_path))
              cn_font_name = 'ChineseFont'
          else:
              cn_font_name = 'Helvetica'

          target_tickers = []
          if os.path.exists('stock_list.txt'):
              with open('stock_list.txt', 'r', encoding='utf-8') as f:
                  for line in f:
                      t = line.strip()
                      if t and not t.startswith('#'):
                          target_tickers.append(t.upper())
          if not target_tickers:
              target_tickers = ["MARA", "IREN", "SOXL", "TQQQ", "MSFT", "META", "APLD", "SPY", "QQQ", "BULL", "URA", "HOOD", "SOFI"]

          CORE_ANCHORS = {"MSFT", "META", "SPY", "QQQ", "URA"}

          # 2. Extract Data Dumps
          db_path = "data/stock_analysis.db"
          db_dump = ""
          if os.path.exists(db_path):
              try:
                  conn = sqlite3.connect(db_path)
                  cursor = conn.cursor()
                  cursor.execute("SELECT name FROM sqlite_master WHERE type='table';")
                  for table_name in cursor.fetchall():
                      t_name = table_name[0]
                      try:
                          cursor.execute(f"SELECT * FROM {t_name} ORDER BY rowid DESC LIMIT 80;")
                          for row in cursor.fetchall():
                              row_str = str(row)
                              if any(ticker in row_str.upper() for ticker in target_tickers):
                                  db_dump += row_str + "\n"
                      except Exception:
                          pass
                  conn.close()
              except Exception as e:
                  print(f"DB read note: {e}")

          import glob
          for f_path in glob.glob("output/**/*", recursive=True) + glob.glob("reports/**/*", recursive=True):
              try:
                  with open(f_path, "r", encoding="utf-8") as rf:
                      content = rf.read()
                      if any(ticker in content.upper() for ticker in target_tickers):
                          db_dump += f"\n--- {f_path} ---\n" + content
              except Exception:
                  pass

          # 3. Batch Fetch Precise Live Prices
          print("-> Fetching live market prices in single batch...")
          live_metrics = {}
          tickers_query = " ".join(target_tickers)
          try:
              download_df = yf.download(tickers_query, period="5d", interval="1d", group_by='ticker', progress=False)
              for t in target_tickers:
                  try:
                      if len(target_tickers) == 1:
                          sub_df = download_df['Close'].dropna()
                      else:
                          sub_df = download_df[t]['Close'].dropna()
                      p_latest = round(float(sub_df.iloc[-1]), 2)
                      p_prev = float(sub_df.iloc[-2]) if len(sub_df) >= 2 else p_latest
                      ret_1d = round(((p_latest - p_prev) / p_prev) * 100, 2)
                      live_metrics[t] = {"price": p_latest, "1d": ret_1d}
                  except Exception:
                      live_metrics[t] = {"price": 0.0, "1d": 0.0}
          except Exception as e:
              print(f"Batch market data fetch warning: {e}")
              for t in target_tickers:
                  live_metrics[t] = {"price": 0.0, "1d": 0.0}

          # 4. Generate Institutional Dual-Language Analyses
          print("-> Requesting Gemini institutional analyses...")
          ai_client = genai.Client(api_key=os.getenv("GEMINI_API_KEY"))

          prompt_en = f"""
You are a senior equity analyst. Review the records below and generate a comprehensive executive summary report in USD covering ALL 13 stocks: {', '.join(target_tickers)}.

Current Live Closing Prices:
{json.dumps(live_metrics, indent=2)}

Strict Structure:
1. Executive Overview: Macro context, AI infrastructure, and asset allocation strategy.
2. Watch List Summary Comparison Table: Markdown table with columns | Ticker | Asset Name | Asset Class | Sentiment Score (1-10) | Rating | Core Signal |
3. Individual Asset Breakdowns: Provide in-depth financial metrics (Share Price in USD, Market Cap, Key Metrics), Technical Trend Status, and Risk Advisory for EVERY single ticker ({', '.join(target_tickers)}).
4. Portfolio Strategy & Tactical Allocation Recommendations.

Raw Input Records:
{db_dump[:18000]}
"""

          prompt_cn = f"""
你是一位資深金融分析師。請審閱以下記錄，並撰寫一份涵蓋全部13檔標的（{', '.join(target_tickers)}）的繁體中文高階投資決策報告。所有價格、目標價、市值與財務數據必須嚴格以 USD ($) 呈現。

即時行情價格:
{json.dumps(live_metrics, indent=2)}

嚴格結構要求:
1. 執行摘要 (Executive Overview): 宏觀環境、AI與算力基建趨勢、資產配置方針。
2. 綜合對比表 (Summary Comparison Table): Markdown表格，欄位為 | 代號 | 資產名稱 | 資產類別 | 情緒評分 (1-10) | 投資評級 | 核心訊號 |。
3. 個股/資產深度解析: 為名單中每一檔標的（{', '.join(target_tickers)}）提供詳細現價(USD)、技術趨勢分析與風險提示。
4. 投資組合構建與戰略配置建議。

原始數據記錄:
{db_dump[:18000]}
"""

          try:
              res_en = ai_client.models.generate_content(
                  model="gemini-2.5-flash",
                  contents=prompt_en
              )
              summary_en = res_en.text
          except Exception as e:
              print(f"Error generating English report: {e}")
              summary_en = "### Executive Summary Report\nAnalysis generated from market records."

          try:
              res_cn = ai_client.models.generate_content(
                  model="gemini-2.5-flash",
                  contents=prompt_cn
              )
              summary_cn = res_cn.text
          except Exception as e:
              print(f"Error generating Chinese report: {e}")
              summary_cn = "### 投資決策報告\n市場分析已完成。"

          # 5. Extract Ratings & Self-Heal Prediction History Baseline
          history_file = "reports_history.json"
          history = {}
          if os.path.exists(history_file):
              try:
                  with open(history_file, "r", encoding="utf-8") as hf:
                      history = json.load(hf)
              except Exception:
                  history = {}

          today_str = date.today().isoformat()
          current_snapshot = {}

          for t in target_tickers:
              cur_p = live_metrics.get(t, {}).get("price", 0.0)
              if cur_p == 0.0:
                  section_match = re.search(rf"(?:###|\b\d+\.\s*|\b){t}\b.*?(?=\n\d+\.|\n###|\Z)", summary_en, re.DOTALL | re.IGNORECASE)
                  if section_match:
                      sec_text = section_match.group(0)
                      p_match = re.search(r'(?:Price|Share Price|Current Price)[^\$\n]*\$\s*([\d\.]+)', sec_text, re.IGNORECASE)
                      if p_match:
                          cur_p = float(p_match.group(1))

              rating_match = re.search(rf"\*\*?{t}\*\*?\s*\|[^\n\|]*\|[^\n\|]*\|[^\n\|]*\|\s*([A-Za-z\s]+?)\s*\|", summary_en)
              cur_rating = rating_match.group(1).strip() if rating_match else "Hold"
              current_snapshot[t] = {"price": cur_p, "rating": cur_rating}

          # Self-healing logic for past dates corrupted with identical prices (e.g., 18.50)
          for p_date, records in list(history.items()):
              if p_date < today_str:
                  price_values = [v.get("price", 0.0) for v in records.values() if isinstance(v, dict)]
                  # If all recorded prices are identical (and > 3 stocks), it was corrupted
                  if len(price_values) > 3 and len(set(price_values)) == 1:
                      print(f"-> Sanitizing corrupted baseline for date {p_date}...")
                      for sym in records.keys():
                          try:
                              hist_close = yf.Ticker(sym).history(period="1mo")['Close']
                              if not hist_close.empty:
                                  records[sym]["price"] = round(float(hist_close.iloc[-3]), 2)
                          except Exception:
                              pass

          history[today_str] = current_snapshot
          with open(history_file, "w", encoding="utf-8") as hf:
              json.dump(history, hf, indent=2, ensure_ascii=False)

          # 6. Generate Tiered Verification Scorecard
          past_dates = sorted([d for d in history.keys() if d < today_str])
          eval_table_en = ""
          eval_table_cn = ""
          
          if past_dates:
              target_date = past_dates[-min(2, len(past_dates))]
              past_records = history[target_date]
              
              eval_table_en = f"## 3-Day Rolling Signal Verification & Accuracy\n"
              eval_table_en += f"### Tracking Window: {target_date} &rarr; {today_str} (With ±1.5% Buffer for Core Anchors)\n\n"
              eval_table_en += f"| Ticker | Tier | Signal Date | Past Signal | Base Price | Current Price | Return % | Outcome |\n| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |\n"
              
              eval_table_cn = f"## 歷史預測回測與信號勝率驗證 (3日週期)\n"
              eval_table_cn += f"### 追蹤時間窗口: {target_date} &rarr; {today_str} (核心底倉含 ±1.5% 緩衝區)\n\n"
              eval_table_cn += f"| 代號 | 分類 | 信號日期 | 前期信號 | 基準價格 | 當前價格 | 實際變動 | 評定結果 |\n| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |\n"
              
              wins, total = 0, 0
              for sym, past in past_records.items():
                  if sym in current_snapshot and past.get("price", 0) > 0 and current_snapshot[sym]["price"] > 0:
                      p_old = past["price"]
                      p_now = current_snapshot[sym]["price"]
                      chg = ((p_now - p_old) / p_old) * 100
                      rating = past.get("rating", "Hold")
                      is_core = sym in CORE_ANCHORS
                      tier_en = "Core" if is_core else "Tactical"
                      tier_cn = "核心" if is_core else "戰術"

                      is_bull = any(w in rating.lower() for w in ["buy", "accumulate", "bull"])
                      is_bear = any(w in rating.lower() for w in ["sell", "reduce", "bear"])
                      
                      if is_bull:
                          if chg > 0:
                              res_en, res_cn, is_win = "Win", "達標", True
                          elif is_core and chg >= -1.5:
                              res_en, res_cn, is_win = "Consolidating", "區間盤整", True
                          else:
                              res_en, res_cn, is_win = "Miss", "未達標", False
                      elif is_bear:
                          if chg < 0:
                              res_en, res_cn, is_win = "Win", "達標", True
                          elif is_core and chg <= 1.5:
                              res_en, res_cn, is_win = "Consolidating", "區間盤整", True
                          else:
                              res_en, res_cn, is_win = "Miss", "未達標", False
                      else:
                          if abs(chg) <= 2.5:
                              res_en, res_cn, is_win = "Range", "符合區間", True
                          else:
                              res_en, res_cn, is_win = "Breakout", "波動突破", False

                      if is_win: wins += 1
                      total += 1

                      eval_table_en += f"| **{sym}** | {tier_en} | {target_date} | {rating} | ${p_old:.2f} | ${p_now:.2f} | {chg:+.2f}% | {res_en} |\n"
                      eval_table_cn += f"| **{sym}** | {tier_cn} | {target_date} | {rating} | ${p_old:.2f} | ${p_now:.2f} | {chg:+.2f}% | {res_cn} |\n"

              win_rate = (wins / total * 100) if total > 0 else 0
              eval_table_en += f"\n> **Model Accuracy Score:** **{win_rate:.1f}% ({wins}/{total})** directional consistency from {target_date} to today.\n\n"
              eval_table_cn += f"\n> **模型綜合勝率:** **{win_rate:.1f}% ({wins}/{total})**（基準對比日期: {target_date}）。\n\n"

          # 7. Render ReportLab PDF Documents
          def generate_pdf(text_content, filename, title_prefix, font_name):
              doc = SimpleDocTemplate(filename, pagesize=letter, rightMargin=32, leftMargin=32, topMargin=32, bottomMargin=32)
              styles = getSampleStyleSheet()
              
              title_style = ParagraphStyle('TitleStyle', parent=styles['Heading1'], fontSize=14, textColor=colors.HexColor('#1a365d'), fontName=font_name, spaceAfter=2)
              subtitle_style = ParagraphStyle('SubTitleStyle', parent=styles['Normal'], fontSize=8.5, textColor=colors.HexColor('#718096'), fontName=font_name, spaceAfter=8)
              heading_style = ParagraphStyle('HeadingStyle', parent=styles['Heading2'], fontSize=10.5, textColor=colors.HexColor('#2d3748'), fontName=font_name, spaceBefore=6, spaceAfter=3)
              body_style = ParagraphStyle('BodyStyle', parent=styles['Normal'], fontSize=8, textColor=colors.HexColor('#2d3748'), fontName=font_name, leading=11, spaceAfter=3)
              th_style = ParagraphStyle('THStyle', parent=styles['Normal'], fontSize=7.5, textColor=colors.white, fontName=font_name)
              td_style = ParagraphStyle('TDStyle', parent=styles['Normal'], fontSize=7.5, textColor=colors.HexColor('#2d3748'), fontName=font_name)

              story = []
              story.append(Paragraph("Daily Stock Analysis Decision Dashboard", title_style))
              story.append(Paragraph(f"{title_prefix} &mdash; {datetime.now().strftime('%d %B %Y')}", subtitle_style))
              story.append(Spacer(1, 4))

              table_data = []
              for line in text_content.split('\n'):
                  l_clean = line.strip()
                  if not l_clean:
                      continue
                  
                  if "|" in l_clean and "---" not in l_clean:
                      cols = [Paragraph(c.strip(), th_style if not table_data else td_style) for c in l_clean.split('|') if c.strip()]
                      if cols:
                          table_data.append(cols)
                      continue
                  elif "|" in l_clean and "---" in l_clean:
                      continue
                  elif table_data and "|" not in l_clean:
                      num_cols = len(table_data[0])
                      col_widths = [45, 45, 55, 55, 55, 55, 50, 60] if num_cols == 8 else [55, 110, 90, 70, 75, 139][:num_cols]
                      t = Table(table_data, colWidths=col_widths)
                      t.setStyle(TableStyle([
                          ('BACKGROUND', (0,0), (-1,0), colors.HexColor('#1a365d')),
                          ('ALIGN', (0,0), (-1,-1), 'LEFT'),
                          ('VALIGN', (0,0), (-1,-1), 'MIDDLE'),
                          ('BOTTOMPADDING', (0,0), (-1,-1), 3),
                          ('TOPPADDING', (0,0), (-1,-1), 3),
                          ('GRID', (0,0), (-1,-1), 0.5, colors.HexColor('#cbd5e0'))
                      ]))
                      story.append(t)
                      story.append(Spacer(1, 6))
                      table_data = []

                  if l_clean.startswith("## ") or l_clean.startswith("# "):
                      story.append(Paragraph(f"<b>{l_clean.replace('#', '').strip()}</b>", heading_style))
                  elif l_clean.startswith("### "):
                      story.append(Paragraph(f"<b>{l_clean.replace('#', '').strip()}</b>", body_style))
                  else:
                      safe_p = l_clean.replace('*', '').replace('&', '&amp;').replace('<', '&lt;').replace('>', '&gt;')
                      story.append(Paragraph(safe_p, body_style))

              if table_data:
                  num_cols = len(table_data[0])
                  col_widths = [45, 45, 55, 55, 55, 55, 50, 60] if num_cols == 8 else [55, 110, 90, 70, 75, 139][:num_cols]
                  t = Table(table_data, colWidths=col_widths)
                  t.setStyle(TableStyle([
                      ('BACKGROUND', (0,0), (-1,0), colors.HexColor('#1a365d')),
                      ('ALIGN', (0,0), (-1,-1), 'LEFT'),
                      ('VALIGN', (0,0), (-1,-1), 'MIDDLE'),
                      ('BOTTOMPADDING', (0,0), (-1,-1), 3),
                      ('TOPPADDING', (0,0), (-1,-1), 3),
                      ('GRID', (0,0), (-1,-1), 0.5, colors.HexColor('#cbd5e0'))
                  ]))
                  story.append(t)

              story.append(Spacer(1, 8))
              story.append(Paragraph(f"<i>Source: Automated Stock Analysis Decision Dashboard, generated on {datetime.now().strftime('%d %B %Y')}.</i>", subtitle_style))
              doc.build(story)

          pdf_en = "Active_Watchlist_Analysis_EN.pdf"
          pdf_cn = "Active_Watchlist_Analysis_CN.pdf"

          print("-> Compiling Dual-Language PDF Reports with Adaptive Scorecards...")
          generate_pdf(eval_table_en + summary_en, pdf_en, "Active Watchlist Summary (English)", 'Helvetica')
          generate_pdf(eval_table_cn + summary_cn, pdf_cn, "Active Watchlist Summary (中文版)", cn_font_name)

          # 8. Email Dispatch
          sender = os.getenv("SMTP_USERNAME")
          receiver = os.getenv("NOTIFY_EMAIL")
          password = os.getenv("SMTP_PASSWORD")

          msg = MIMEMultipart()
          msg["Subject"] = f"Daily Stock Watchlist Analysis & Accuracy Scorecard (EN & CN) - {datetime.now().strftime('%Y-%m-%d')}"
          msg["From"] = sender
          msg["To"] = receiver

          email_body = "Hello,\n\nPlease find attached today's complete dual-language Daily Stock Analysis PDF Summaries, featuring accurate per-ticker live prices, multi-page deep dives, and adaptive signal verification scorecards.\n\nBest regards,\nYour Trading Assistant Bot"
          msg.attach(MIMEText(email_body, "plain", "utf-8"))

          for pdf_file in [pdf_en, pdf_cn]:
              if os.path.exists(pdf_file):
                  with open(pdf_file, "rb") as attachment:
                      part = MIMEBase("application", "octet-stream")
                      part.set_payload(attachment.read())
                  encoders.encode_base64(part)
                  part.add_header("Content-Disposition", f"attachment; filename= {pdf_file}")
                  msg.attach(part)

          with smtplib.SMTP("smtp.gmail.com", 587) as server:
              server.starttls()
              server.login(sender, password)
              server.sendmail(sender, receiver, msg.as_string())
          print("Dual-language emails with accurate live pricing and scorecards sent successfully!")
          EOF

      - name: Commit and push updated prediction history
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add reports_history.json || true
          git commit -m "chore: update daily stock prediction history baseline [skip ci]" || echo "No changes to commit"
          git push || echo "No remote changes to push"
